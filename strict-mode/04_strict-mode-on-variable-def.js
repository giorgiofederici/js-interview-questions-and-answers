'use strict';

var value = 0;
valeu = 1;
if (value > 1) {
   console.log('Value is greater than 1');
}

// The typo is like a variable definition without var keyword.
// Not allowed in not strict mode.
// An error will appear in the console.