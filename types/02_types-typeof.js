typeof(1); // "number"

typeof("a"); // "string"

typeof(true); // "boolean"

typeof(undefined); // "undefined"

typeof(null); // "object" <-- not correct!!!! It should be null, but can't be changed now, it's too late

typeof({}); // "object"