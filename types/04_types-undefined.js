

var a; // not initialized to any value

console.log(a); // --> undefined

/**
 * undefined -> it's used by the JavaScript engine to inform you that:
 * - uninitialized variables, so variables like a which have not been initialized with another value.
 * - missing parameters to functions
 * - unknown variables and unknown properties.
*/